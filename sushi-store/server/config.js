// Конфигурация сервера. Здесь можно хранить секреты и другие
// параметры. В реальном проекте эти значения лучше помещать в
// переменные окружения (process.env) и не хранить в репозитории.

module.exports = {
  // Секретный ключ для генерации токенов. В production следует
  // генерировать уникальную строку и хранить её безопасно.
  SECRET_KEY: 'supersecretkey'
};